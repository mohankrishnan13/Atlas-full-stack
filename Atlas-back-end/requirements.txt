# ══════════════════════════════════════════════════════════════════════════════
# ATLAS SOC Dashboard — Python Dependencies
# Generated from full codebase analysis (Python 3.12)
# ══════════════════════════════════════════════════════════════════════════════

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1          # ASGI server with websocket + http/2 extras

# ── Pydantic / Settings ────────────────────────────────────────────────────────
pydantic==2.10.3                   # Data validation (v2 — field_validator API)
pydantic[email]==2.10.3            # EmailStr support used in routes_auth.py
pydantic-settings==2.6.1          # BaseSettings + SettingsConfigDict (core/config.py)

# ── Database — Async ORM ────────────────────────────────────────────────────────
sqlalchemy==2.0.36                 # ORM: Mapped, mapped_column, async_sessionmaker
asyncpg==0.30.0                    # Async PostgreSQL driver (postgresql+asyncpg://)

# ── Database — Sync (Alembic migrations only) ───────────────────────────────────
psycopg2-binary==2.9.10            # Sync driver for DATABASE_URL_SYNC (Alembic CLI)
alembic==1.14.0                    # Schema migrations

# ── Authentication / Security ──────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode (create_access_token)
passlib[bcrypt]==1.7.4             # Password hashing: hash_password / verify_password
bcrypt==4.2.1                      # bcrypt backend for passlib

# ── HTTP Client (Wazuh active-response calls) ───────────────────────────────────
requests==2.32.3                   # WazuhActions.run_command (core/wazuh_client.py)
urllib3==2.2.3                     # InsecureRequestWarning suppression (wazuh_client)

# ── Environment / Config ────────────────────────────────────────────────────────
python-dotenv==1.0.1               # load_dotenv() in core/config.py

# ── Async Utilities ─────────────────────────────────────────────────────────────
anyio==4.7.0                       # Async backend used by FastAPI / Starlette

# ── AWS S3 Cold-Storage (optional, enabled via S3_ENABLED=true) ─────────────────
boto3==1.35.83                     # S3IngestCursor background polling task

# ── LLM Copilot — Ollama (optional) ────────────────────────────────────────────
httpx==0.28.1                      # Async HTTP client for Ollama REST calls

# ── Data Processing (used by query/log services referenced in routes_dashboard) ──
pandas==2.2.3                      # In-memory analytics engine (log_loader, _build_*_df)
numpy==2.2.0                       # Numerical ops used alongside pandas

# ── Dev / Debug ─────────────────────────────────────────────────────────────────
# Uncomment in non-production builds only
# watchfiles==0.24.0               # Hot-reload for uvicorn --reload
# pytest==8.3.4
# pytest-asyncio==0.24.0
# httpx==0.28.1                    # Also used as test client via TestClient
