# ATLAS Backend Dependencies — PostgreSQL + Velociraptor Stack
# Python 3.11+
# Generated for the refactored MVP replacing Elasticsearch/Wazuh

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
numpy
pandas
requests

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── PostgreSQL (async driver + ORM) ──────────────────────────────────────────
# asyncpg: the fastest async PostgreSQL driver for Python
asyncpg==0.29.0
# SQLAlchemy with async support
sqlalchemy[asyncio]==2.0.30
# psycopg2 is needed only for Alembic migrations (sync)
psycopg2-binary==2.9.9

# ── Database Migrations ───────────────────────────────────────────────────────
alembic==1.13.1

# ── HTTP Client (for Ollama LLM Copilot + Velociraptor gRPC bridge) ──────────
httpx==0.27.0

# ── Security / Auth ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<4.0.0
python-multipart==0.0.9

# ── Logging ───────────────────────────────────────────────────────────────────
python-json-logger==2.0.7

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
# httpx doubles as async test client for FastAPI
# (already listed above, no duplicate needed)

# ── Future: Kafka Consumer (uncomment when moving to live ingestion) ──────────
# aiokafka==0.10.0

# ── Future: Velociraptor gRPC client (uncomment when connecting live server) ──
# grpcio==1.63.0
# grpcio-tools==1.63.0
# pyvelociraptor>=0.1.0  # Community wrapper — check latest version

# ── AWS S3 Cold-Storage Ingestion ─────────────────────────────────────────────
# boto3 is always installed; the S3 task only activates when S3_ENABLED=true.
boto3==1.34.120
botocore==1.34.120

# ── REMOVED dependencies (do NOT re-add these) ───────────────────────────────
# elasticsearch[async]  ← replaced by PostgreSQL
# redis                 ← no longer needed (session state in DB)
# scikit-learn          ← ML engine removed from MVP (future roadmap item)
# pandas                ← removed with ML engine
# numpy                 ← removed with ML engine
